import { IntroductionQuestion } from './introduction-question';

describe('IntroductionQuestion', () => {
  it('should create an instance', () => {
    expect(new IntroductionQuestion()).toBeTruthy();
  });
});
